# Generated by chezmoi. Do not edit directly.
# Run `chezmoi edit ~/.zprofile` to modify the source template.

# ===== Common =====


{{- if eq .chezmoi.os "darwin" }}

# ===== macOS =====

# OrbStack
[ -f "$HOME/.orbstack/shell/init.zsh" ] && source "$HOME/.orbstack/shell/init.zsh"

# Homebrew (Apple Silicon: /opt/homebrew, Intel: /usr/local)
for brewbin in /opt/homebrew/bin/brew /usr/local/bin/brew; do
  if [ -x "$brewbin" ]; then
    eval "$("$brewbin" shellenv)"
    break
  fi
done

{{- else if eq .chezmoi.os "linux" }}

# ===== Linux =====

# Homebrew (Linuxbrew: 共有 /home/linuxbrew か個人 ~/.linuxbrew)
for brewbin in /home/linuxbrew/.linuxbrew/bin/brew "$HOME/.linuxbrew/bin/brew"; do
  if [ -x "$brewbin" ]; then
    eval "$("$brewbin" shellenv)"
    break
  fi
done

{{- end }}
